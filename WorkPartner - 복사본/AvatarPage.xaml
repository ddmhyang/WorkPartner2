<UserControl x:Class="WorkPartner.AvatarPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://expression.blend/2008"
             xmlns:local="clr-namespace:WorkPartner"
             mc:Ignorable="d"
             d:DesignHeight="526" d:DesignWidth="682"
             Background="{DynamicResource MainBackgroundBrush}">
    <UserControl.Resources>
        <Style x:Key="CategoryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10" Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SelectedCategoryButtonStyle" TargetType="Button" BasedOn="{StaticResource CategoryButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentColorBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

    </UserControl.Resources>

    <Viewbox Stretch="Uniform">
        <Canvas Width="682" Height="526">

            <TextBlock Canvas.Left="40" Canvas.Top="40" Text="아바타" FontSize="18" FontFamily="Pretendard" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
            <TextBlock Canvas.Left="40" Canvas.Top="70" Text="획득한 코인으로 나만의 아바타를 꾸며보세요!" FontSize="12" FontFamily="Pretendard" Foreground="{DynamicResource SecondaryTextBrush}"/>

            <Border Canvas.Left="40" Canvas.Top="110" Width="300" Height="380" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2" CornerRadius="10" Background="{DynamicResource SecondaryBackgroundBrush}">
                <local:CharacterDisplay x:Name="CharacterPreview" />
            </Border>

            <StackPanel Canvas.Left="360" Canvas.Top="110" Width="282">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="보유 코인:" VerticalAlignment="Center" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <Image Source="/images/coin.png" Width="16" Height="16" Margin="10,0,5,0"/>
                    <TextBlock x:Name="CoinDisplay" Text="0" VerticalAlignment="Center" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                </StackPanel>

                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <StackPanel x:Name="CategoryPanel" Orientation="Horizontal"/>
                </ScrollViewer>

                <Border Width="282" Height="300" Margin="0,15,0,0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2" CornerRadius="10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <WrapPanel x:Name="ItemPanel" Margin="10"/>
                    </ScrollViewer>
                </Border>
            </StackPanel>
        </Canvas>
    </Viewbox>
</UserControl>
<Page x:Class="WorkPartner.AvatarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WorkPartner"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="AvatarPage">

    <Page.Resources>
        <!-- 상점 아이템을 표시하기 위한 데이터 템플릿 -->
        <DataTemplate x:Key="ShopItemTemplate">
            <Button Style="{StaticResource ItemButtonStyle}" Click="ItemButton_Click" Margin="5" Width="120" Height="150">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- 아이템 이미지 -->
                    <Image Source="{Binding ImagePath}" Stretch="Uniform" Margin="5" />

                    <!-- 아이템 이름 -->
                    <TextBlock Grid.Row="1" Text="{Binding Name}" HorizontalAlignment="Center" Margin="0,5,0,0"/>

                    <!-- 아이템 가격 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,2,0,5">
                        <Image Source="/images/coin.png" Width="12" Height="12" Margin="0,0,3,0" />
                        <TextBlock Text="{Binding Price}" FontWeight="SemiBold" />
                    </StackPanel>

                    <!-- 색상 변경 버튼 (필요시 활성화) -->
                    <!-- 
                    <Button Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5"
                            Width="20" Height="20" Content="C" Click="ColorButton_Click"
                            ToolTip="색상 변경" />
                    -->
                </Grid>
            </Button>
        </DataTemplate>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Background="{DynamicResource BackgroundColor}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="300"/>
                <ColumnDefinition Width="Auto" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <!-- 왼쪽: 아이템 선택 영역 -->
            <TabControl Grid.Column="0" Margin="10" Style="{StaticResource CustomTabControlStyle}">
                <TabItem Header="앞머리">
                    <ScrollViewer>
                        <ItemsControl x:Name="HairFrontItemsControl" ItemTemplate="{StaticResource ShopItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="뒷머리">
                    <ScrollViewer>
                        <ItemsControl x:Name="HairBackItemsControl" ItemTemplate="{StaticResource ShopItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="눈">
                    <ScrollViewer>
                        <ItemsControl x:Name="EyeItemsControl" ItemTemplate="{StaticResource ShopItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="입">
                    <ScrollViewer>
                        <ItemsControl x:Name="MouthItemsControl" ItemTemplate="{StaticResource ShopItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="옷">
                    <ScrollViewer>
                        <ItemsControl x:Name="ClothesItemsControl" ItemTemplate="{StaticResource ShopItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="방석">
                    <ScrollViewer>
                        <ItemsControl x:Name="CushionItemsControl" ItemTemplate="{StaticResource ShopItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="장신구">
                    <ScrollViewer>
                        <ItemsControl x:Name="AccessoriesItemsControl" ItemTemplate="{StaticResource ShopItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="배경">
                    <ScrollViewer>
                        <ItemsControl x:Name="BackgroundsItemsControl" ItemTemplate="{StaticResource ShopItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
            </TabControl>

            <!-- 오른쪽: 캐릭터 디스플레이 및 정보 -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,20,0">
                    <Image Source="/images/coin.png" Width="20" Height="20" Margin="0,0,5,0" />
                    <TextBlock x:Name="CoinText" Text="0" FontWeight="Bold" VerticalAlignment="Center" FontSize="16"/>
                </StackPanel>

                <local:CharacterDisplay x:Name="characterDisplay" Grid.Row="1" Margin="20" />

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="20">
                    <Button x:Name="RevertButton" Content="되돌리기" Style="{StaticResource ActionButton}" Margin="0,0,10,0" Click="RevertButton_Click" />
                    <Button x:Name="SaveButton" Content="저장하기" Style="{StaticResource PrimaryActionButton}" Click="SaveButton_Click" />
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>


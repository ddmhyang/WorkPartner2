<Window x:Class="WorkPartner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkPartner"
        mc:Ignorable="d"
        Title="WorkPartner" Height="720" Width="1280"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">
    <Border Background="{StaticResource AppBackgroundBrush}" CornerRadius="10" BorderBrush="#D0D0D0" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Navigation Bar -->
            <Border Grid.Column="0" Background="#F0F0F0" CornerRadius="10,0,0,10" MouseDown="Border_MouseDown">
                <StackPanel Width="180">
                    <TextBlock Text="WorkPartner" FontSize="20" FontWeight="Bold" Margin="20,20,20,30" HorizontalAlignment="Center"/>

                    <Button Tag="Dashboard" Content="📊 대시보드" Click="NavButton_Click"/>
                    <Button Tag="Analysis" Content="📈 분석" Click="NavButton_Click"/>
                    <Button Tag="Avatar" Content="🎨 아바타 꾸미기" Click="NavButton_Click"/>
                    <Button Tag="Settings" Content="⚙️ 설정" Click="NavButton_Click"/>

                </StackPanel>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Top Bar for Window Controls -->
                <Border Grid.Row="0" Height="30" Background="Transparent" MouseDown="Border_MouseDown">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="─" Click="MinimizeButton_Click" Style="{StaticResource WindowControlButton}"/>
                        <Button Content="✕" Click="CloseButton_Click" Style="{StaticResource WindowControlButton}"/>
                    </StackPanel>
                </Border>

                <!-- Page Content -->
                <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>

            </Grid>
        </Grid>
    </Border>
</Window>


<Window x:Class="WorkPartner.MiniTimerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WorkPartner"
        Title="WorkPartner Timer" Height="100" Width="240"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border x:Name="TimerBorder" Background="#88000000" CornerRadius="10">
        <Grid>
            <Image x:Name="BackgroundImage" Stretch="UniformToFill" Opacity="0.5"/>

            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 좌측: 캐릭터 -->
                <local:CharacterDisplay x:Name="MiniCharacterDisplay" Grid.Column="0" Width="60" Height="60" VerticalAlignment="Center" Visibility="Visible"/>

                <!-- 우측: 정보 스택 -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                    <!-- 닉네임, 레벨, 재화 정보 -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock x:Name="NicknameTextBlock" Text="Nickname" Foreground="White" FontSize="11" FontWeight="Bold"/>
                            <TextBlock Text=" | " Foreground="#AAAAAA" FontSize="11"/>
                            <TextBlock x:Name="LevelTextBlock" Text="Lv.1" Foreground="White" FontSize="11"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Image Source="/images/coin.png" Width="10" Height="10" Margin="0,0,2,0"/>
                            <TextBlock x:Name="MoneyTextBlock" Text="0" Foreground="#FFD700" FontSize="11" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>

                    <!-- 현재 과목 정보 -->
                    <TextBlock x:Name="InfoTextBlock" Text="과목 정보" Foreground="#DDDDDD" FontSize="10" Margin="0,5,0,2"/>

                    <!-- 현재 과목 누적 시간 -->
                    <TextBlock x:Name="TimeTextBlock" Text="00:00:00" Foreground="White" FontSize="22" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>


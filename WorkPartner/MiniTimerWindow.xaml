<Window x:Class="WorkPartner.MiniTimerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WorkPartner"
        Title="WorkPartner Timer" Height="80" Width="220"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border x:Name="TimerBorder" Background="#88000000" CornerRadius="10">
        <Grid>
            <Image x:Name="BackgroundImage" Stretch="UniformToFill" Opacity="0.5"/>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <local:CharacterDisplay x:Name="MiniCharacterDisplay" Width="40" Height="40" Visibility="Collapsed"/>

                <TextBlock x:Name="TimeTextBlock" Text="00:00:00" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock x:Name="InfoTextBlock" Text="과목 정보" Foreground="White" FontSize="10" HorizontalAlignment="Center" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
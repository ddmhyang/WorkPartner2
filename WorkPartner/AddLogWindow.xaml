<Window x:Class="WorkPartner.AddLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://expression.blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="학습 기록 추가/수정" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="과목:" Margin="5"/>
        <ComboBox Grid.Row="0" x:Name="TaskComboBox" ItemsSource="{Binding TaskItems}" DisplayMemberPath="Text" SelectedItem="{Binding SelectedTaskItem}" Margin="60,5,5,5"/>

        <TextBlock Grid.Row="1" Text="시작 시간:" Margin="5"/>
        <xctk:DateTimePicker Grid.Row="1" x:Name="StartTimePicker" Value="{Binding StartTime}" Margin="60,5,5,5"/>

        <TextBlock Grid.Row="2" Text="종료 시간:" Margin="5"/>
        <xctk:DateTimePicker Grid.Row="2" x:Name="EndTimePicker" Value="{Binding EndTime}" Margin="60,5,5,5"/>

        <TextBlock Grid.Row="3" Text="집중도:" Margin="5"/>
        <Slider Grid.Row="3" x:Name="FocusSlider" Minimum="1" Maximum="5" Value="{Binding FocusScore}" IsSnapToTickEnabled="True" TickFrequency="1" Margin="60,5,5,5" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="저장" Width="75" Margin="5" Click="SaveButton_Click"/>
            <Button x:Name="DeleteButton" Content="삭제" Width="75" Margin="5" Click="DeleteButton_Click"/>
            <Button x:Name="CancelButton" Content="취소" Width="75" Margin="5" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
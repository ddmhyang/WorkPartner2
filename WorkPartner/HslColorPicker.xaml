<UserControl x:Class="WorkPartner.HslColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="140" d:DesignWidth="200">
    <StackPanel>
        <TextBlock Text="Hue (색상)" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
        <Slider x:Name="HueSlider" Minimum="0" Maximum="360" 
                ValueChanged="Slider_ValueChanged"/>

        <TextBlock Text="Saturation (채도)" FontSize="10" Margin="0,5,0,0" Foreground="{DynamicResource SecondaryTextBrush}"/>
        <Slider x:Name="SaturationSlider" Minimum="0" Maximum="1" Value="1"
                ValueChanged="Slider_ValueChanged"/>

        <TextBlock Text="Lightness (명도)" FontSize="10" Margin="0,5,0,0" Foreground="{DynamicResource SecondaryTextBrush}"/>
        <Slider x:Name="LightnessSlider" Minimum="0" Maximum="1" Value="0.5"
                ValueChanged="Slider_ValueChanged"/>

        <Rectangle x:Name="PreviewRectangle" Height="20" Margin="0,5,0,0" Stroke="{DynamicResource BorderBrush}" StrokeThickness="1" RadiusX="3" RadiusY="3">
            <Rectangle.Fill>
                <SolidColorBrush x:Name="PreviewBrush" Color="White" />
            </Rectangle.Fill>
        </Rectangle>

    </StackPanel>
</UserControl>